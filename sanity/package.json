{
  "name": "portfolio",
  "private": true,
  "version": "1.0.0",
  "main": "package.json",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "yarn copy-env-files && sanity dev",
    "start": "yarn copy-env-files && sanity start",
    "build": "yarn copy-env-files && sanity build",
    "deploy": "yarn copy-env-files && sanity deploy",
    "deploy-graphql": "yarn copy-env-files && sanity graphql deploy",
    "copy-env-files": "cp ../.env* ./"
  },
  "keywords": [
    "sanity"
  ],
  "dependencies": {
    "@sanity/vision": "^3.23.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-is": "^18.2.0",
    "react-player": "^2.14.1",
    "sanity": "^3.23.4",
    "sanity-plugin-asset-source-ogimage": "^2.0.0-2",
    "styled-components": "^6.0.7"
  },
  "devDependencies": {
    "@sanity/eslint-config-studio": "^3.0.1",
    "@types/react": "^18.0.25",
    "eslint": "^8.6.0",
    "prettier": "^3.0.2",
    "typescript": "^5.1.6"
  },
  "prettier": {
    "semi": false,
    "printWidth": 100,
    "bracketSpacing": false,
    "singleQuote": true
  }
}
